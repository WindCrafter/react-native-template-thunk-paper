import React, { useCallback, useRef } from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

interface IBTextMulti {
  text?: string;
}

export default function BTextMulti() {
  return (
    <View>
      <Text>Hello</Text>
    </View>
  );
}
